# ============================================================
# LOCAL DEV requirements (your machine)
# Backend = Ollama, plus the evaluation + reporting tooling.
# Lightweight: NO torch / transformers here (those are deploy-only).
# Install with:  pip install -r requirements.txt
# ============================================================

# --- Core app ---
streamlit>=1.40
python-dotenv>=1.0

# --- OSS model backend (local) ---
ollama>=0.3            # talks to the local Ollama server over HTTP

# --- Frontier-tier hosted model (comparison + LLM-as-judge during eval) ---
groq>=0.11          # Llama-3.3-70B via Groq (primary frontier backend + judge)
google-genai>=0.3   # optional alternative frontier backend (Gemini)

# --- Evaluation & reporting ---
pandas>=2.2
matplotlib>=3.9
